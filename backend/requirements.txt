# ============================================================
# IMPORTANT: Create and activate a virtual environment first.
#
#   /usr/bin/python3 -m venv .venv
#
# Then install dependencies:
#
#   For llama-cpp-python, the standard pip install does NOT
#   include CUDA support. You must build it from source with
#   the flags below — do NOT use plain pip install:
#
#   PATH=/usr/local/cuda/bin:$PATH \
#   CMAKE_ARGS="-DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=87" \
#   .venv/bin/pip install llama-cpp-python==0.3.31
#
#   Then install the rest normally:
#
#   .venv/bin/pip install -r requirements.txt
# ============================================================

opencv-python-headless==4.13.0.92
fastapi>=0.138.0
uvicorn[standard]>=0.49.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0
python-multipart>=0.0.9
